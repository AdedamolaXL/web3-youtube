{
  "name": "web3-youtube-new",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ adedamolaxl/web3-youtube-new",
    "create-local": "graph create --node http://localhost:8020/ adedamolaxl/web3-youtube-new",
    "remove-local": "graph remove --node http://localhost:8020/ adedamolaxl/web3-youtube-new",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 adedamolaxl/web3-youtube-new",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.65.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
